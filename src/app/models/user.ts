export class User {
    id: string;
    name: string;
    email: string;
    password: string
}

export class vacancyType {
    types = [
        { id: 1, name: 'featured' },
        { id: 2, name: '' }
    ]
}